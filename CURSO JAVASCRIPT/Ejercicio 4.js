let altura_cm = 160;
let altura_m = 1.6;
let peso_kg = 90.8;

console.log(altura_m.toFixed(0));
let altura_red = Math.ceil(altura_m);
console.log(altura_red);

console.log(parseInt(peso_kg));
let peso_red = Math.floor(peso_kg);
console.log(peso_red);
let sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
